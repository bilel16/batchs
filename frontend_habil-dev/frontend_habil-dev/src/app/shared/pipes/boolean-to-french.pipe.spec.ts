import { BooleanToFrenchPipe } from './boolean-to-french.pipe';

describe('BooleanToFrenchPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanToFrenchPipe();
    expect(pipe).toBeTruthy();
  });
});
