/*package com.bna.smile.model.domainetraitementfichier;

import com.bna.commun.util.ContextHandler;

import com.oxia.fwk.context.Context;

import java.util.Date;

import javax.sql.DataSource;

import org.springframework.jdbc.core.JdbcTemplate;

public class AjoutExcep {

   /** * * * * * * * * * * * * * *
     * Ajouter une exception     *                      
     * @author JAOUALI Yossri    *
     * @since 28/10/2010         *
     * * * * * * * * * * * * * * **/

/*
    JdbcTemplate jt;
    Context context = ContextHandler.getContext();
    DataSource dataSource = (DataSource)context.getBean("dataSource");

    public Long getLastValueSeqException() {
        jt = new JdbcTemplate(dataSource);
        String requete = "select SEQ_BATCH_EXCEPTION_PLAC.nextval from dual";
        Long nbre = (Long)jt.queryForObject(requete, Long.class);
        return nbre;
    }

    public void ajouterExceptionBatchAccuse(Long NumSeq, Date dateSys, 
                                            Date dateComp, String libBatch, 
                                            String libExcp, Long codeStrc) {

        jt.update("INSERT INTO BATCH_EXCEPTION_PLAC (NUM_SEQ_BATE, DAT_SYST_BATE,DAT_COMP_BATE,LIB_TPBM_BATE,LIB_EXP_BATE,COD_STRC_BATE) VALUES(?,?,?,?,?,?)", 
                  new Object[] { new Long(NumSeq), dateSys, dateComp, libBatch, 
                                 libExcp, new Long(codeStrc) });
    }
}

*/