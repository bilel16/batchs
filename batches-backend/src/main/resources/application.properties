# Application
spring.application.name=BATCHES
server.port=${BATCHES_SERVER_PORT:8000}

# Oracle Database (JPA auto-config excluded — connection kept for future use)
# spring.datasource.url=${BATCHES_DB_URL}
# spring.datasource.username=${BATCHES_DB_USERNAME}
# spring.datasource.password=${BATCHES_DB_PASSWORD}
# spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
# spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.show-sql=false
# spring.jpa.hibernate.ddl-auto=none
# spring.jpa.open-in-view=false
spring.main.allow-bean-definition-overriding=true
spring.main.allow-circular-references=true
spring.jackson.serialization.fail-on-empty-beans=false

# CORS
cors.allowed-origins=${BATCHES_FRONTEND_ORIGIN:http://localhost:4200}

# Environment (dev / prod)
env.local=${BATCHES_ENV:dev}
local.ip=192.168.251.246

# External authentication service (same as habil)
urlAuth=${BATCHES_URL_AUTH:http://localhost:9999/authenticate}
urlroles=${BATCHES_URL_ROLES:http://localhost:9999/roles}
codeApp=${BATCHES_CODE_APP:BATCHES}

# JWT
jwt.secret=${BATCHES_JWT_SECRET:CjF0o0dmEibgEsydS+ijCbgmkOFn9xrUqXuvUOW4yFh5763yygAZH1XK2qCqsDD6rk7Zb/Cd/gM3pTdVqnOtYA==}
jwt.expiration=3600000

# Upload
spring.servlet.multipart.max-file-size=128MB
spring.servlet.multipart.max-request-size=128MB
spring.servlet.multipart.enabled=true

# Logging
logging.level.com.bna.batches=INFO
logging.level.org.springframework.web=INFO
